version: "3.9"
networks:
  secret-sharing-dev:
    driver: bridge
services:
  app:
    container_name: secret-sharing
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:80"
    networks:
      - secret-sharing-dev
    depends_on:
      - postgres_db
  postgres_db:
    container_name: postgres
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: secret-sharing
    ports:
      - "5432:5432"
    networks:
      - secret-sharing-dev
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
